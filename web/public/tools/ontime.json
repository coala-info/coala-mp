{
  "id": "ontime",
  "category": "CLI",
  "name": "ontime",
  "description": "The ontime utility extracts subsets of Nanopore reads from sequencing files based on specific time durations or absolute timestamps. Use when user asks to inspect file timeframes, subset reads by duration, filter data using absolute UTC timestamps, or extract reads relative to the end of a sequencing run.",
  "overview": "The `ontime` utility allows for the precise extraction of Nanopore reads by leveraging the `start_time` metadata found in read headers. It is designed to help researchers determine the optimal sequencing duration for their specific applications by allowing them to \"slice\" a single long sequencing run into various time-based subsets. It supports both relative durations (e.g., \"the first 2 hours\") and absolute UTC timestamps.",
  "homepage": "https://github.com/mbhall88/ontime",
  "validation": "PASS",
  "conda_home": "https://anaconda.org/channels/bioconda/packages/ontime/overview",
  "github": "https://github.com/mbhall88/ontime",
  "conda_downloads": "9.0K",
  "conda_downloads_num": 9000,
  "last_updated": "2025-08-01",
  "cwl_count": 1,
  "has_skill": true,
  "runtime_summary": [],
  "report": {
    "runtime_summary_table": [],
    "tool_names": [
      "ontime"
    ],
    "description": "Extract subsets of ONT (Nanopore) reads based on time",
    "docker_image": "quay.io/biocontainers/ontime:0.3.1--h031d066_0",
    "homepage": "https://github.com/mbhall88/ontime",
    "validation": "PASS",
    "conda_home": "https://anaconda.org/channels/bioconda/packages/ontime/overview",
    "github": "https://github.com/mbhall88/ontime",
    "conda_downloads": "9.0K",
    "last_updated": "2025-08-01",
    "skill_generated": false,
    "validation_run": "not_done"
  },
  "cwl_files": [
    "ontime.cwl"
  ],
  "skill_file": "skills/SKILL.md",
  "skills_repo_link": null,
  "cwls_repo_link": null,
  "skill_markdown": "# ontime\n\n## Overview\n\nThe `ontime` utility allows for the precise extraction of Nanopore reads by leveraging the `start_time` metadata found in read headers. It is designed to help researchers determine the optimal sequencing duration for their specific applications by allowing them to \"slice\" a single long sequencing run into various time-based subsets. It supports both relative durations (e.g., \"the first 2 hours\") and absolute UTC timestamps.\n\n## Core Workflows\n\n### 1. Inspecting File Timeframes\nBefore subsetting, always check the temporal range of your input file to understand the start and end points of the sequencing run.\n```bash\nontime --show input.fastq\n```\n\n### 2. Extracting by Duration\nDurations are the most common way to subset data. Units like `h` (hours), `m` (minutes), and `s` (seconds) can be combined.\n- **First N hours**: `ontime --to 2h in.fq`\n- **After the first hour**: `ontime --from 1h in.fq`\n- **Specific window**: `ontime --from 1h30m --to 3h in.fq`\n\n### 3. Relative Offsets from End-of-Run\nUse negative durations to subset reads relative to the end of the sequencing session.\n- **Exclude the last hour**: `ontime --to -1h in.fq`\n- **The final 30 minutes**: `ontime --from -30m in.fq`\n\n### 4. Absolute Timestamp Filtering\nWhen you need to match sequencing data to a specific external event, use RFC3339-compliant timestamps (UTC).\n```bash\nontime --from 2022-12-12T20:00:00Z --to 2022-12-12T21:00:00Z in.fq\n```\n\n## Expert Tips and Best Practices\n\n- **Input/Output Formats**: `ontime` supports FASTQ, FASTA, BAM, and SAM. However, it does not perform format conversion. If your input is BAM, your output must be BAM. Use `samtools` if you need to convert formats after subsetting.\n- **Compression**: Save disk space by outputting directly to compressed formats. `ontime` detects compression from the file extension or via the `-O` flag.\n  - Example: `ontime --to 5h -o output.fq.gz in.fq`\n- **UTC Requirement**: All timestamps provided to `--from` or `--to` must be in UTC. If your lab notes are in local time, convert them to UTC before running the command.\n- **Metadata Dependency**: This tool relies on the `start_time=` (FASTQ/FASTA) or `st:Z:` (BAM/SAM) tags. If reads have been processed by tools that strip this metadata, `ontime` will not be able to filter them.\n- **Dorado BAMs**: `ontime` is compatible with BAM files generated by the Dorado basecaller, which includes the necessary start time tags.\n\n## Reference documentation\n- [ontime GitHub Repository](./references/github_com_mbhall88_ontime.md)\n- [ontime Bioconda Overview](./references/anaconda_org_channels_bioconda_packages_ontime_overview.md)",
  "skill_front_matter": {
    "name": "ontime",
    "description": "The ontime utility extracts subsets of Nanopore reads from sequencing files based on specific time durations or absolute timestamps. Use when user asks to inspect file timeframes, subset reads by duration, filter data using absolute UTC timestamps, or extract reads relative to the end of a sequencing run.",
    "homepage": "https://github.com/mbhall88/ontime"
  }
}