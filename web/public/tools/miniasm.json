{
  "id": "miniasm",
  "category": "CLI",
  "name": "miniasm",
  "description": "Miniasm is a minimalist, ultrafast assembler that produces assembly graphs from long-read sequencing data using an overlap-layout paradigm without a consensus step. Use when user asks to assemble long reads into unitigs, generate an assembly graph in GFA format, or perform rapid de novo assembly of PacBio or Nanopore data.",
  "overview": "Miniasm is a minimalist, high-speed assembler designed for long-read sequencing data. It follows the Overlap-Layout-Consensus (OLC) paradigm but uniquely omits the consensus step to achieve \"ultrafast\" performance. The tool takes all-vs-all read self-mappings (typically generated by minimap2) and raw reads to produce an assembly graph in GFA format. Because it does not perform error correction, the resulting unitigs have the same error rate as the raw input reads and must be polished using downs",
  "homepage": "https://github.com/lh3/miniasm",
  "validation": "PASS",
  "conda_home": "https://anaconda.org/channels/bioconda/packages/miniasm/overview",
  "github": "https://github.com/lh3/miniasm",
  "conda_downloads": "113.8K",
  "conda_downloads_num": 113800,
  "last_updated": "2025-04-22",
  "cwl_count": 1,
  "has_skill": true,
  "runtime_summary": [],
  "report": {
    "runtime_summary_table": [],
    "tool_names": [
      "miniasm"
    ],
    "description": "miniasm: option requires an argument -- 'h'\nSee miniasm.1 for detailed description of the command-line options.",
    "docker_image": "quay.io/biocontainers/miniasm:0.3_r179--ha92aebf_0",
    "homepage": "https://github.com/lh3/miniasm",
    "validation": "PASS",
    "conda_home": "https://anaconda.org/channels/bioconda/packages/miniasm/overview",
    "github": "https://github.com/lh3/miniasm",
    "conda_downloads": "113.8K",
    "last_updated": "2025-04-22",
    "skill_generated": false,
    "validation_run": "not_done"
  },
  "cwl_files": [
    "miniasm.cwl"
  ],
  "skill_file": "skills/SKILL.md",
  "skills_repo_link": null,
  "cwls_repo_link": null,
  "skill_markdown": "# miniasm\n\n## Overview\nMiniasm is a minimalist, high-speed assembler designed for long-read sequencing data. It follows the Overlap-Layout-Consensus (OLC) paradigm but uniquely omits the consensus step to achieve \"ultrafast\" performance. The tool takes all-vs-all read self-mappings (typically generated by minimap2) and raw reads to produce an assembly graph in GFA format. Because it does not perform error correction, the resulting unitigs have the same error rate as the raw input reads and must be polished using downstream tools.\n\n## Core Workflow\nMiniasm requires a two-step process: overlapping and layout.\n\n1. **Overlapping (using minimap2)**\n   Generate the required PAF (Pairwise Mapping Format) file.\n   * **PacBio:** `minimap2 -x ava-pb -t8 reads.fq reads.fq | gzip -1 > overlaps.paf.gz`\n   * **Nanopore:** `minimap2 -x ava-ont -t8 reads.fq reads.fq | gzip -1 > overlaps.paf.gz`\n\n2. **Layout (using miniasm)**\n   `miniasm -f reads.fq overlaps.paf.gz > assembly.gfa`\n\n## Common CLI Patterns\n* **Standard Assembly:**\n  `miniasm -f <reads.fastq> <mappings.paf> > <output.gfa>`\n* **Memory Optimization (2-pass mode):**\n  Use `-R` to reduce peak memory usage, which is helpful for larger genomes.\n  `miniasm -R -f reads.fq overlaps.paf > assembly.gfa`\n* **GFA1 Compatibility:**\n  Use `-p` to specify the output type. The `sg` (string graph) option is often used for GFA1 compatibility.\n  `miniasm -p sg -f reads.fq overlaps.paf > assembly.gfa`\n\n## Expert Tips and Best Practices\n* **Mandatory Polishing:** Since miniasm does not have a consensus step, the output GFA contains raw read sequences. You **must** use a tool like Racon, Medaka, or Pilon after assembly to achieve acceptable base accuracy.\n* **Input Requirements:** Miniasm expects all-vs-all mappings. If the PAF file only contains partial mappings or is missing self-mappings, the assembly will be fragmented or empty.\n* **Repeat Resolution:** Miniasm is prone to collapsing repeats or segmental duplications that are longer than the input reads. It is most effective for high-coverage bacterial genomes where long reads can span most repeat regions.\n* **Filtering:** For very high coverage datasets, you can improve speed and potentially assembly quality by filtering for the longest reads (e.g., top 30-50x coverage) before running the overlap step.\n* **Circularization:** For bacterial genomes, look for \"circular\" tags in the GFA output or use tools like `minidot` (included in the repository) to visualize the assembly graph and check for circularity.\n\n## Reference documentation\n- [Miniasm GitHub Repository](./references/github_com_lh3_miniasm.md)\n- [Miniasm Releases and Versioning](./references/github_com_lh3_miniasm_tags.md)",
  "skill_front_matter": {
    "name": "miniasm",
    "description": "Miniasm is a minimalist, ultrafast assembler that produces assembly graphs from long-read sequencing data using an overlap-layout paradigm without a consensus step. Use when user asks to assemble long reads into unitigs, generate an assembly graph in GFA format, or perform rapid de novo assembly of PacBio or Nanopore data.",
    "homepage": "https://github.com/lh3/miniasm"
  }
}