{
  "id": "r-bcbiosinglecell",
  "category": "R",
  "name": "r-bcbiosinglecell",
  "description": "This R package facilitates the post-processing, quality control, and visualization of single-cell RNA-seq data processed with the bcbio-nextgen pipeline. Use when user asks to load bcbio-nextgen outputs into R, perform automated quality control, filter low-quality cells, or generate standardized single-cell visualizations.",
  "overview": "`bcbiosinglecell` is an R package designed to facilitate the analysis of single-cell RNA-seq (scRNA-seq) data generated by the bcbio-nextgen pipeline. It provides a structured way to import bcbio outputs into R as a `bcbioSingleCell` object (which extends the `SingleCellExperiment` class), enabling seamless integration with Bioconductor workflows. The package focuses on automated quality control reporting, data normalization, and exploratory data analysis.",
  "homepage": "",
  "validation": "",
  "conda_home": "",
  "github": "",
  "conda_downloads": "",
  "conda_downloads_num": 0,
  "last_updated": "",
  "cwl_count": 0,
  "has_skill": true,
  "runtime_summary": [],
  "report": {
    "runtime_summary_table": [],
    "tool_names": [],
    "description": "",
    "docker_image": "",
    "homepage": "",
    "validation": "",
    "conda_home": "",
    "github": "",
    "conda_downloads": "",
    "last_updated": "",
    "skill_generated": true,
    "validation_run": "not_done"
  },
  "cwl_files": [],
  "skill_file": "skills/SKILL.md",
  "skills_repo_link": null,
  "cwls_repo_link": null,
  "skill_markdown": "# r-bcbiosinglecell\n\nname: r-bcbiosinglecell\ndescription: R package for post-processing and visualization of single-cell RNA-seq data processed with bcbio. Use this skill when analyzing single-cell datasets (e.g., 10x Genomics) that have been processed through the bcbio-nextgen pipeline, specifically for loading data into R, performing quality control (QC), and generating standardized visualizations.\n\n# r-bcbiosinglecell\n\n## Overview\n`bcbiosinglecell` is an R package designed to facilitate the analysis of single-cell RNA-seq (scRNA-seq) data generated by the bcbio-nextgen pipeline. It provides a structured way to import bcbio outputs into R as a `bcbioSingleCell` object (which extends the `SingleCellExperiment` class), enabling seamless integration with Bioconductor workflows. The package focuses on automated quality control reporting, data normalization, and exploratory data analysis.\n\n## Installation\nTo install the package from Bioconductor:\n```R\nif (!requireNamespace(\"BiocManager\", quietly = TRUE))\n    install.packages(\"BiocManager\")\nBiocManager::install(\"bcbiosinglecell\")\n```\n\n## Key Workflows\n\n### 1. Loading Data\nThe primary entry point is the `bcbioSingleCell()` constructor. Point it to the directory containing the bcbio-nextgen final output.\n\n```R\nlibrary(bcbiosinglecell)\n\n# Load the dataset\nbcb <- bcbioSingleCell(\n    uploadDir = \"bcbio_output/final\",\n    sampleMetadataFile = \"metadata.csv\"\n)\n\n# Inspect the object\nprint(bcb)\n```\n\n### 2. Quality Control (QC)\nThe package provides functions to visualize common scRNA-seq metrics.\n\n- **Plotting Metrics**: Use `plotMetrics()` to see a summary of reads, genes, and mitochondrial content.\n- **Specific QC Plots**:\n  - `plotGenesPerCell(bcb)`\n  - `plotUMIsPerCell(bcb)`\n  - `plotMitoRatio(bcb)`\n  - `plotNovelty(bcb)` (log10 genes per UMI)\n\n### 3. Filtering\nFilter the object based on QC thresholds to remove low-quality cells.\n\n```R\nbcb_filtered <- filterCells(\n    bcb,\n    minUMIs = 500,\n    minGenes = 200,\n    maxMitoRatio = 0.10\n)\n```\n\n### 4. Normalization and Dimensionality Reduction\nThe package supports standard Bioconductor methods (like `scran`) and provides wrappers for visualization.\n\n- **Normalization**: `normalize(bcb)`\n- **PCA**: `plotPCA(bcb)`\n- **t-SNE/UMAP**: `plotTSNE(bcb)` or `plotUMAP(bcb)`\n\n### 5. Marker Analysis and Visualization\n- **Gene Expression**: `plotExpression(bcb, genes = c(\"GeneA\", \"GeneB\"))`\n- **Heatmaps**: `plotMarkerHeatmap(bcb)`\n\n## Tips\n- **Object Class**: The `bcbioSingleCell` object is compatible with any function that accepts a `SingleCellExperiment`.\n- **Metadata**: Ensure your `metadata.csv` contains a `sampleID` column that matches the bcbio output folder names.\n- **Reporting**: Use `prepareSingleCellReport()` to generate an automated R Markdown HTML report of the QC metrics.\n\n## Reference documentation\n- [bcbiosinglecell](./references/home_page.md)",
  "skill_front_matter": {
    "name": "r-bcbiosinglecell",
    "description": "This R package facilitates the post-processing, quality control, and visualization of single-cell RNA-seq data processed with the bcbio-nextgen pipeline. Use when user asks to load bcbio-nextgen outputs into R, perform automated quality control, filter low-quality cells, or generate standardized single-cell visualizations.",
    "homepage": "https://cran.r-project.org/web/packages/bcbiosinglecell/index.html"
  }
}