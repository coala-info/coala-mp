{
  "id": "kmer-counter",
  "category": "CLI",
  "name": "kmer-counter",
  "description": "The kmer-counter tool transforms nucleotide sequences into a fixed-size feature matrix stored as a NumPy file for machine learning applications. Use when user asks to count k-mers in FASTA files, generate a k-mer count matrix for TensorFlow or NumPy, or extract sequence features for genomic analysis.",
  "overview": "The `kmer-counter` tool is a Rust-based utility optimized for speed and minimal memory footprint. It transforms nucleotide sequences into a fixed-size feature matrix stored as a NumPy `.npy` file. Unlike tools designed for k-mer discovery or assembly, this counter is specifically built for feature extraction, where every possible k-mer of a specified length is assigned a deterministic index in the output array, facilitating direct loading into TensorFlow or NumPy.",
  "homepage": "https://github.com/CobiontID/kmer-counter",
  "validation": "PASS",
  "conda_home": "https://anaconda.org/channels/bioconda/packages/kmer-counter/overview",
  "github": "https://github.com/CobiontID/kmer-counter",
  "conda_downloads": "786",
  "conda_downloads_num": 786,
  "last_updated": "2025-11-15",
  "cwl_count": 1,
  "has_skill": true,
  "runtime_summary": [],
  "report": {
    "runtime_summary_table": [],
    "tool_names": [
      "kmer-counter"
    ],
    "description": "Tally nucleotide counts in multi-entry fasta",
    "docker_image": "quay.io/biocontainers/kmer-counter:0.1.2--h4349ce8_0",
    "homepage": "https://github.com/CobiontID/kmer-counter",
    "validation": "PASS",
    "conda_home": "https://anaconda.org/channels/bioconda/packages/kmer-counter/overview",
    "github": "https://github.com/CobiontID/kmer-counter",
    "conda_downloads": "786",
    "last_updated": "2025-11-15",
    "skill_generated": false,
    "validation_run": "not_done"
  },
  "cwl_files": [
    "kmer-counter.cwl"
  ],
  "skill_file": "skills/SKILL.md",
  "skills_repo_link": null,
  "cwls_repo_link": null,
  "skill_markdown": "# kmer-counter\n\n## Overview\n\nThe `kmer-counter` tool is a Rust-based utility optimized for speed and minimal memory footprint. It transforms nucleotide sequences into a fixed-size feature matrix stored as a NumPy `.npy` file. Unlike tools designed for k-mer discovery or assembly, this counter is specifically built for feature extraction, where every possible k-mer of a specified length is assigned a deterministic index in the output array, facilitating direct loading into TensorFlow or NumPy.\n\n## Usage Instructions\n\n### Basic Command Pattern\nTo generate k-mer counts for a sequencing file, use the following structure:\n```bash\nkmer-counter --file <input.fasta> --ids <ids.txt> --klength <int> --out <output.npy> --collapse <0|1>\n```\n\n### Parameter Guidance\n- `--file`: Path to the input nucleotide sequences. Supports both plain FASTA and gzipped (`.gz`) files.\n- `--klength`: The length of the k-mers to count. The default is 4. Note that memory usage scales exponentially with this value ($4^k$).\n- `--collapse`: \n    - `1` (Default): Canonicalizes k-mers, meaning a k-mer and its reverse complement are counted together under the same key.\n    - `0`: Counts k-mers and their reverse complements separately.\n- `--ids`: Generates a plain text file containing the sequence identifiers in the same order as the rows in the output matrix.\n- `--out`: The path for the resulting `.npy` file containing the 32-bit integer count matrix.\n\n### Interpreting the Output\nThe output matrix rows correspond to the sequence records in the input file. The columns correspond to the k-mer keys generated by the Cartesian product of \"ACGT\" in alphabetical order.\n\nTo reconstruct the k-mer keys in Python for a given `k`:\n```python\nfrom itertools import product\nk = 4\nkeys = [\"\".join(i) for i in product(\"ACGT\", repeat=k)]\n```\n\n## Expert Tips and Best Practices\n\n- **Memory Efficiency**: The tool is designed to handle hundreds of millions of reads with very low peak memory (e.g., ~7MB for $k=4$). Memory requirements are determined by the k-mer space ($4^k$), not the number of sequences.\n- **Dense vs. Sparse Data**: This tool is not recommended for very large k-mer sizes (e.g., $k > 12$) where the resulting matrix would be extremely sparse. It allocates a slot for every possible k-mer regardless of whether it is observed.\n- **Downstream Integration**: Since the output is a standard `.npy` file, use `numpy.load('output.npy')` in Python to immediately begin analysis. The row order is guaranteed to match the input FASTA and the generated IDs file.\n- **Canonicalization**: Always use `--collapse 1` unless your specific biological question requires distinguishing between a sequence and its reverse complement (e.g., strand-specific analysis).\n\n## Reference documentation\n- [kmer-counter GitHub Repository](./references/github_com_CobiontID_kmer-counter.md)\n- [kmer-counter Bioconda Overview](./references/anaconda_org_channels_bioconda_packages_kmer-counter_overview.md)",
  "skill_front_matter": {
    "name": "kmer-counter",
    "description": "The kmer-counter tool transforms nucleotide sequences into a fixed-size feature matrix stored as a NumPy file for machine learning applications. Use when user asks to count k-mers in FASTA files, generate a k-mer count matrix for TensorFlow or NumPy, or extract sequence features for genomic analysis.",
    "homepage": "https://github.com/CobiontID/kmer-counter",
    "metadata": "",
    "docker_image": "quay.io/biocontainers/kmer-counter:0.1.2--h4349ce8_0"
  }
}